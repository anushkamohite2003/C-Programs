#include<stdio.h>
#include<conio.h>

int main()
{
    char ch = '\0';
    int No = 55;
    int size = 0;

    size = sizeof(No);

    printf("\n size for integer No is = %d",Size);

    printf("\n Size of Character variable ch is = %d,");

    getch();
    return 0;
}
